generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  email         String  @unique
  access_token  String?
}

model Aas {
  idShort       String
  catalogue     String?
  id            String  @unique
  data          String?
}